
#Kafka配置
kafka:
  consumer:
    config:
      timeout: 15000
      concurrency: 3
      servers: 10.253.106.164:9092
      msginterval: 100
      msgtimeout: 15000
      presentGroup: presentGroup
      offset: earliest
    topic:
      registerComplete: registerComplete
